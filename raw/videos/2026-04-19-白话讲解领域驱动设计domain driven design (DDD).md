---
title: "白话讲解领域驱动设计domain driven design (DDD)"
url: "https://www.bilibili.com/video/BV11u411176h/?spm_id_from=333.1391.0.0&vd_source=2430e934c270f1656bc475b83b6e6701"
bvid: "BV11u411176h"
cid: "490682708"
author: "JimmyCoding"
upload_date: "2022-01-23"
subtitle_lang: "中文"
created: "2026-04-19"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=508441679&bvid=BV11u411176h&cid=490682708&page=1&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 字幕

`00:01` 大家好
`00:01` 我是吉米
`00:02` 今天呢我想跟大家聊一聊追夺命driven design啊
`00:06` 这个设计模式
`00:08` 我第一次听说董命driven design是在我们公司内部的一个技术分享会上
`00:14` 当时我们组有一个工程师想要向大家推荐dm driven design这个设计模式
`00:22` 由于这个这个技术名词还算一个有技术热度的东西
`00:28` 所以很多人参加了这个会议
`00:30` 我当时呢印象很深
`00:32` 因为整场会开下来
`00:35` 整整个场面呢是十分安静的
`00:38` 主持过这类会议的人一定有类似的感觉
`00:41` 那么就是开会其实最怕安静了
`00:46` 也就是没人问得出来问题
`00:48` 就代表大家没有听懂你在讲什么
`00:52` 我当时听下来呢
`00:54` 内心里是有一种说不出的愤怒
`00:57` 因为我心里在想
`00:59` 大哥你都在讲些什么
`01:01` 讲了一堆这样的概念
`01:04` 比如说repository
`01:05` entity
`01:06` aggregate rt
`01:08` 这都是些什么概念呢
`01:10` 我没有搞懂
`01:11` 首先为什么需要这些概念
`01:13` 那么这整场会议却在大部分时间在讲解
`01:17` 这都是些什么概念
`01:19` 还要说服我们在实际代码中要用到这些概念
`01:24` 所以呢最后的结果大家确实都没有听懂
`01:29` dm driven design
`01:31` 也别提什么对项目进行dm driven design的改造了
`01:37` 不过我这个人对技术还是有一点较真精神
`01:41` 后来还是研究了一些关于dd d的设计思路
`01:45` 发现dd d这个东西确实是一个好东西
`01:49` 对我的代码实现呢还是有很大的启发和提高
`01:54` 所以今天想特地出一期视频
`01:57` 来讲解一下d d d这个这个设计模式的底层思路
`02:03` 那么如果你也想实打实的利用d d d提高自己的项目工程能力
`02:10` 这期视频呢请一定看完
`02:12` 程序员呢在不同的阶段都是要学习一些相应阶段需要的编程设计模型
`02:20` 嗯
`02:20` 打个比方来讲
`02:22` 在我们刚开始编程的时候呢
`02:24` 我认为最重要的设计模式
`02:27` 那么就是对应的就是叫做o o p面向对象编程对吧
`02:32` 那么在当时呢我们感觉自己会写的累啊
`02:37` 那就觉得自己有点不得了了
`02:39` 那么在下一个阶段呢
`02:42` 就是我们需要开始学习一些常见的这种design pattern
`02:47` 嗯
`02:48` 比如说工厂模型啊
`02:50` 比如说适配器模型
`02:52` 装饰模型等等等等
`02:54` 这个时候呢我们可能就是天天会捧一捧着一个design pattern这种书
`03:01` 可以每天寻找其中的模板来
`03:05` 能能够帮助我们啊更好地实现一些代码的编写
`03:09` 那么再到后来呢
`03:11` 进入下一个阶段
`03:12` 我们可能就会突然意识到啊
`03:15` 有一个东西啊叫做solid
`03:19` solid呢对应了其中的五种啊
`03:24` 那么会发现这个东西啊真的是一个好东西
`03:28` 用这种思路不仅可以帮助自己写好代码啊
`03:33` 另外呢可以很很轻松的给别人留出来一大堆的comment
`03:39` 因为那么这个阶段呢学完这些东西
`03:42` 这个时候呢可能程序员呢就会觉得有点飘了
`03:46` 认为自己很牛逼
`03:48` 自己的代码写得非常好啊
`03:51` 别人的代码都是垃圾
`03:53` 其实呢到目前这个阶段
`03:56` 很多这样的设计模型呢还是基于项目细节层的设计
`04:02` 比方说几个类是怎么设计的等等
`04:06` 如果这个时候呢你如果很顺利的能够成为项目组里的一个技术骨干
`04:13` 也就是你们组里的主要马力的时候呢
`04:17` 你可能会接到更多的任务
`04:20` 开始设计一些宏观项目层的设计
`04:23` 这个时候设计呢就不单是以及一些类的设计这么简单了
`04:29` 这个设计更多的是需要多个模块之间的设计
`04:34` 以及很多内部外部系统的集成
`04:38` 这种情况下呢
`04:40` 你可能就会陷入到一个迷茫
`04:43` 迷茫期
`04:44` 你会感觉怎么之前学的东西有点不好使了
`04:49` 但其实也不是不好使
`04:50` 是不够用了
`04:52` 那么如果你处于这个阶段
`04:55` 这个时候你就急需要学习一下domain dream design这个思路了
`05:01` 抖命driven design其实是一些设计思想的集合
`05:06` 如果你去看很多文章对抖命driven design的介绍
`05:10` 会提到这是一种什么基于斗命expert对抖命设计之类的介绍
`05:17` 那么这种介绍你看完基本就晕了
`05:21` 这篇文章会把抖命追问点赞
`05:23` 这个理论讲得有一些邪乎
`05:26` 但其实我自己总结呢
`05:37` 就是在英文中模块这个词就是module
`05:41` 你如何modualize你的软件系统
`05:45` 说白了就是把一个复杂的软件应用系统的其中各个部分
`05:52` 更加进行很好的拆解和封装
`05:56` 以达到高内聚低耦合这样的效果
`06:00` 那么这个时候有观众可能会说了
`06:03` 封装这个概念是不是也太老生常谈了
`06:07` 扯d d d这么个复杂的理论干嘛
`06:10` 确实dvd呢确实是对风光模块化的一套体系思想
`06:18` 你可能平时都已经在利用到了这些理论
`06:22` 但是呢这个理论就能够很好地把这些良好的设计规律给总结出来
`06:31` 我们先来从架构的宏观层次来看一下我们我们的模块化
`06:38` 那么我们目前大部分的项目是基于一个三层的架构
`06:44` 对吧
`06:45` 嗯打个例子来比方
`06:49` 也就是我们平常所说到的这个叫做per 3 presentation层
`06:59` 那么关于这一层
`07:03` 它也就是包含了我们平常所说到的这个前端界面啊
`07:09` 包括这些controller啊
`07:13` 嗯包括controller
`07:16` 也就是我们经常说的backend for front对吧
`07:19` 它虽然代码是在后端
`07:22` 但是它的作用呢是完全和前端进行交互对接的作用
`07:27` 所以呢这些一般都会被算作在presentation这一层
`07:33` 那么接下来中间的一层
`07:36` 也就是我们平常啊程序员最最最常面对的这一层
`07:41` 也就是
`07:44` 我们的application这一层啊
`07:47` 也就是服务应用这一层嗯
`07:50` 正常来讲呢
`07:51` 我们的c r u d程序员啊
`07:53` 大部分的逻辑也就是会发生在这一层啊
`07:59` 这一层呢也就是我们啊传统意义上所所说的各种各样的service
`08:07` service呢正常来讲呢会做一些validation的工作啊
`08:13` 也就是各种验证以及c r u d啊等等等等的
`08:17` 这种与数据库的交互和和一些呃存储读写的这方面的逻辑
`08:25` 那么接下来最后一层呢
`08:28` 也就是啊我们通通可以引用为另一层叫做infrastructure层
`08:35` 那么这个infrastructure成嗯
`08:39` 也就是我们正常来讲我们使用到的数据库也好啊
`08:45` 啊我们使用到的第三方的library啊等等等等
`08:50` 这些呢都算在这个infrastructure这一层
`08:55` 那么如果我们来看这种架构
`08:58` 它的依赖关系
`09:00` 它是一个很明显的自上而下的依赖的这种关系对吧
`09:06` 一般呢这种前端和control这一层会直接通过啊各种各样的service to service的这种request
`09:16` 访问到我们的application
`09:18` 那么application呢会把会执行我们的业务逻辑以及与数据库的一个交互
`09:25` 把结果返回给这个presentation层
`09:29` 那么同理呢application呢对啊
`09:31` infrastructure也是有一个比较强的依赖关系
`09:34` 它会利用我们的啊
`09:36` 比方说这种d dio啊
`09:39` data access object啊去进行数据库的读取和存储
`09:44` 那么application呢也相应的对这些database和第一方
`09:51` 无论是还有第三方的这这种library啊
`09:54` 有一个强依赖的关系
`09:56` 那么这样的项目结构它其实呢是有一定的问题存在的
`10:03` 首先第一个问题就是application这一层呢容易过于庞大和臃肿
`10:12` 那么这样造成的问题就是业务逻辑容易不够清晰
`10:19` 这样的话对于一个项目的维护是很困难的
`10:23` 无论是新加入的工程师
`10:25` 还是以红城市
`10:28` 想基于现有的功能啊
`10:30` 亲加一些新的这种啊功能
`10:33` 它都是很难清晰地看到业务逻辑是怎么怎么样的
`10:38` 因为你这一层太大
`10:41` 如果没有进行很好的这种模块化
`10:44` 所有的业务逻辑都堆在一起
`10:47` 那么这时候呢你维护他是很困难的
`10:50` 我相信大家在自己的经历和一些项目中啊
`10:54` 或多或少会见到一些这样的问题
`10:59` 那么第二个问题呢就是infrastructure对infrastructure的依赖性过高
`11:05` 那么如果我们这个infrastructure想要进行一些技术层次的升级
`11:12` 那么就会非常非常的困难啊
`11:15` 这个怎么讲呢
`11:16` 升级一般呢包括比如说database的升级
`11:21` 某一天呢你们决定采用更先进的一种database对吧
`11:26` 亦或是各种第三方的这种轮子
`11:29` 你如果要对它升级这层强的依赖关系
`11:33` 就会对你的业务造成一种一种阻碍作用
`11:38` 那么很多项目可能就会因为这样的一种原因而放弃了他们的升级计划
`11:44` 来继续维护他们的这个lexy啊
`11:47` 这种系统
`11:49` 那么第三点呢就是当你的这种项目变得过于庞大
`11:55` 那么采用最新的这种技术架构
`11:59` 比如说微服务就会非常非常的困难
`12:02` 那么微服务的一大思路呢就是能够对一个庞大的应用程序进行分解
`12:09` 分解之后
`12:10` 各个微服务能够独立的运行和升级管理
`12:16` 那么这种新型的这种微服务的架构啊
`12:21` 能够帮助我们进行更快的项目迭代
`12:24` 以及更小的这种影响力来更新一些服务
`12:29` 比如说你可以只更新一个服务
`12:32` 对而对其他的服务没有影响等等
`12:35` 那么种种这些问题呢
`12:37` 其实就会造成了这个传统的三层架构
`12:42` 逐渐的啊会对我们的项目啊
`12:46` 尤其是逐渐演进的项目造成很大的问题
`12:49` 好那么这个时候我们的抖bedriven design啊
`12:53` 提出了这个三层架构呢需要被更新一下
`12:58` 更新过后的架构啊
`13:00` 可以更加的适用于不同的场景
`13:03` 好
`13:04` 那么这时候我们首先呢先把原有的这三个层拷贝一下
`13:20` 我们来看一下d d d啊是怎么改造我们这个三层的架构的
`13:27` 那么首先d d d提到了这一层的架构啊
`13:32` application到infer这一层啊
`13:34` 应该中间新加一个层啊
`13:37` 叫做董命层
`13:40` 抖命层呢也就是我们啊常说的领域
`13:43` 那么关于为什么要增加赌命这一层呢
`13:47` 首先我们的应用程序它的逻辑啊
`13:53` 总的来讲可以被分为两大类
`13:56` 一类呢是固定不变的常用的啊逻辑
`14:00` 比如说我们的一个啊订单正常下单的这种业务逻辑啊
`14:07` 他的可核心内容可能是存在相似的啊
`14:12` 甚至是固定不变的
`14:14` 那么这一这样的逻辑是要被放在董命这一层的
`14:20` 那么application这一层呢主要那么就用来负责接入不同的use case啊
`14:28` 也就是应用场合
`14:30` 那么这个应用场合怎么来理解呢
`14:34` 就是指一些基于不同的场合会产生的不同的业务逻辑
`14:41` 打个比方嗯
`14:44` 如果这个网络应用用户从手机端接入和从网络端接入
`14:52` 他的业务逻辑可能会产生些许的不同
`14:55` 那么这些逻辑就应该放在这个application层
`15:00` 再打一个另外的比方
`15:02` 同样一个网站
`15:04` 比如电商网站
`15:06` 如果是用户登录这个网站和和这个店家卖
`15:14` 也就是卖产品的这些店家登录这个网站
`15:17` 它的业务逻辑也是不相同的
`15:21` 那么这样的处理不同的逻辑的这些部分就要被放在这一个application application层
`15:30` 那么简单归纳呢就是application层啊
`15:34` 存放于不同场景可能会产生不同
`15:39` 对这个董命产生不同影响的业务逻辑
`15:43` 而董命呢是用来存放那些相对稳定啊
`15:49` 更多时候是完全不会变动的逻辑的这一部分的逻辑
`15:54` 现在我们来看一下infer这一层的情况
`15:58` 之前呢我们面对info这一层最大的问题就是其复杂的依赖关系
`16:04` 由于info这一层呢大部分包含的是第三方的类库
`16:09` 以及一些著名的开源轮子
`16:11` 比如说消息队列呀
`16:14` 缓存啊等等
`16:15` 所以我们是希望我们的应用程序的所有层尽可能的独立于
`16:21` 甚至说与英特尔这一层尽可能的解耦
`16:25` 那么这也就要求infer这一层在整体架构当中的位置
`16:31` 其发生了变化
`16:33` 我们呢先把infer这一层的结构在架构啊图中画出来
`16:44` 这个时候我们可以看到呢
`16:46` infa是处于与原有三层相对并行的位置
`16:51` 那么它们的依赖关系呢也会发生一个变化
`16:57` 这时候他们的依赖关系应该是infra依赖于已有的三层而已
`17:03` 有三层对infer这一层没有任何的依赖关系
`17:07` 那么这是怎么样做到的呢
`17:10` 其实就是添加一层称为防腐层的对隔离
`17:16` 关于这一点的实现呢
`17:18` 在是后面的视频当中啊
`17:20` 我会详细讲到
`17:22` 那么我们现在再来看一下层与层之间的数据传输的接口啊
`17:29` 到底是什么
`17:31` 首先呢这个presentation和application之间呢
`17:35` 它传输的啊应该是一种叫做rust d t o的东西啊
`17:43` 这个rust呢就是我们常说的rust api啊
`17:47` api接口层
`17:48` 以及或者有的时候我们定义a i呢是以swagger的方式定义的
`17:53` 那么dt呢就是data transfer object
`17:57` 这个呢说的就是我们以及和用户之间的接口数据定义是什么
`18:04` 这种呢一般呢是不能够啊轻易修改的
`18:09` 每次对这个dt增加数据或者修改数据都需要有非常严格的啊
`18:16` api版本的这种控制策略在里头
`18:20` 那么接下来呢是application和斗命他们之间传输的啊
`18:26` 数主要数据是什么呢
`18:28` 他们主要传输的就是我们经常说的所说的entity
`18:33` 那么这个entity为什么和rust的d t o不是同样的数据定义呢
`18:40` 这个是因为还是像之前所讲的
`18:44` 和用户接口的数据定义是不能随意修改的
`18:48` 但是抖命层的业务逻辑可是随时可能发生变化的
`18:53` 比如说这个entity可能承载了更多的状态
`18:58` 或者某一个业务的逻辑部分啊
`19:01` 发生了一些修改等等等等
`19:04` 这些原因都有可能导致entity的定义是变化的
`19:09` 所以为了让他们这两个数据结构解耦啊
`19:14` 最好我们把它定义成两种不同的数据的定义
`19:18` 那么在application这一层呢
`19:21` 第一步所要做的就是将rush的dt转换成我们entity的数据定义
`19:28` 而且一般随着这个转化的过程
`19:32` 我们将antity所有关于数据结构这种数据的标准
`19:40` 合法性的检查也封装其中
`19:43` 这样呢随着antity的初始化
`19:45` 我们可以最大程度的保证数据的这种合理合法性
`19:54` 那么接下来是抖命层和数据库啊
`19:58` 在英法层这一层呢
`19:59` 也就是主要代表着我们数据库
`20:01` 它们之间传输的呢是一种叫做database d t o
`20:11` 那么这个database dt呢主要呢是对应于我们刚才这个rust啊
`20:18` a p i层的dt
`20:20` 那么这一层呢也就是我们所常常定义的数据库schema啊
`20:26` 这种schema和russ的地球是同样的道理
`20:30` 是不能够轻易被修改的
`20:33` 每一次的数据库的修改
`20:35` 我相信大家都有类似的这种migration的经验
`20:39` 那么这一层呢也需要保证相对的稳定
`20:43` 所以当抖m做任何数据库的存储啊
`20:48` 以及读取的时候
`20:49` 都需要介于entity和database dt之间做一个转换好
`20:55` 那么现在呢我们对于我们的三层架构啊进行了一个很好的这种升级
`21:01` 现在呢经过升级之后
`21:03` 它由传统的三层架构啊
`21:06` 变成了这样一种更加符合dd这种设计思路的一种架构
`21:12` 那么有了这个清晰的架构
`21:15` 我们基于这个架构啊
`21:16` 又能怎么样来切切实实的对我们的项目进行一个模块化呢
`21:23` 那么对于任何一个模块化
`21:25` 当你开始一个软件项目的时候
`21:29` 你最需要设计好的是这一个项目啊
`21:33` 有哪几个project啊
`21:35` 他们都处于什么样的一种关系
`21:38` 他们的依赖关系是什么样的
`21:40` 以及啊甚至说他们的命名
`21:44` 他们的需要在哪些文件夹等等等等
`21:48` 当只有这些框架搭好之后
`21:51` 你的项目才可以在一个相对可控的范围分发给不同的开发人员
`21:58` 进行各自的开发
`22:00` 那么针对于这一点
`22:02` 我们来依次看看对应于各一层
`22:05` 我们需要设计出怎么样的一些模块儿
`22:09` 首先呢是关于presentation这一层
`22:12` presentation这一层呢一般来讲呢就像我们所说的啊
`22:17` 我们的controller也好
`22:19` 我们的啊ui也好
`22:22` 我们的backend for front啊等等等等这样的一种说法的这些模块也好
`22:27` 我们呢都给他将其放在一个叫做web这样的模块下
`22:36` 那么在这样的模块下呢
`22:39` 有我们有像我们之前啊提到的
`22:45` 所有的这种controller也好
`22:48` 啊啊b f f也好
`22:55` 那么他呢都是在这样一层模块中进行封装
`22:59` 那么接下来呢就是我们的application这一层
`23:03` 那么application这一层呢
`23:06` 一般来讲呢我们呢也是需要一个单独的模块来进行封装
`23:13` 那么这时候呢基于我们的场景啊
`23:18` 就像我们刚才所说的application代表了不同的使用场景
`23:22` 基于我们的场景
`23:23` 我们需要根据不同的场景创建啊
`23:27` 属于他们的application啊
`23:30` 那么在这里呢我们呢就给他啊先命名一个啊
`23:37` 就比如说web application这一层
`23:41` 如果你有手机的application
`23:42` 你有各种不同的场景的application
`23:45` 那么都需要定义在这个地方
`23:47` 那么它主要包括的是什么呢
`23:50` 它主要包括的就是我们通常所说的这种service
`24:02` 各类啊各类的这种service
`24:06` 比如说订单系统啊
`24:07` 你的订单的这种service啊
`24:10` 你的这种啊收费系统啊
`24:13` 你的payment service等等等等
`24:15` 各类service都是定义在这个包中
`24:19` 但是呢就像我们刚刚所看到的
`24:24` 基于这个presentation和application之间
`24:28` 它们都是基于这个rust啊d t o进行的一种交互
`24:33` 那么这个d t o呢也需要定义出来
`24:37` 那么这里我们的建议呢是d t o定义在application啊这一层
`24:42` 一般呢所有的d t o最好被单独定义在一个包中
`24:48` 那么它代表了一种啊最低的一种dependency的一种单元
`24:53` 方便呢所有别的包引用它的时候
`24:56` 不会造成一种叫啊circular dependency啊
`25:01` 就是环形引用这样的一种情况发生
`25:04` 那么我们在这里呢也把这种啊
`25:10` 这种呢包呢单独定义出来
`25:13` 在a p a p p中呢
`25:15` 它一般的啊命名规则呢就是你相对应的application啊
`25:21` 加一个share的这样的一种名字啊
`25:25` 这个呢也就是根据你们team团队的命名的这种准则啊
`25:31` 来命名就好了啊
`25:32` 有的时候他叫contract
`25:33` 有的时候叫啊
`25:34` 叫什么什么都可以啊
`25:36` 我们一般呢a p p呢是基于a a p p掉
`25:40` 就加一个share啊这样的命名规则
`25:42` 那么它这里呢就是我们所需要的所有的rust啊
`25:46` d t o的这种这种定义
`25:51` 那么这就是我们application层啊
`25:54` 主要是说呃定义的两大类的一个包
`25:57` 那么抖命层呢思路比较类似啊
`26:01` 赌命层呢首先呢是需要有一个啊包呢来定义我们所需要的懂命service
`26:09` 那么这里这时候呢斗命层一般呢就是命名为你们的相对应的抖命啊
`26:18` 比如说如果是订单呢
`26:19` 就叫叫叫叫叫做order啊
`26:22` 如果呢是支付系统呢
`26:24` 就叫做支付的这种赌命
`26:26` 那么抖命层一般啊包含的是董命service以及数据仓库啊
`26:35` 以及啊这种基于董命城的叫啊一些业务逻辑被封装在一个包中
`26:43` 那么这里头主要包含的就是一种呢叫做repair
`26:51` repulsory的定义
`26:53` 那么repulsory的定义呢一般呢就是它是基于一个更上层啊
`27:00` 对于d t o啊的一种封装层
`27:03` 便于呢我们对所有数据进行的一种访问啊
`27:09` 基于这个reaction呢也有啊
`27:11` 对于复杂的场景也有
`27:14` 也有的时候我们需要定义一种叫factory啊
`27:17` factory来进行reporter进一步的一种组装
`27:21` 那么在这里我们就啊对这一层细节呢不再进行讨论啊
`27:26` 那么只需要理解repulse是我们抖命访问数据啊
`27:31` 读取数据
`27:33` 增删改查所有的一个经过他的一种啊repository的一种一个layer
`27:39` 那么除了report队呢
`27:41` 一般来讲呢还会定义我们刚才所提到的董命service
`27:45` 董命service呢一般建议呢被命名为啊相对应的这个抖命加manager啊
`27:52` 这么这么一种名字
`27:54` 那么抖命service呢就一般呢是会被share给application这一层
`28:00` 用于直接访问懂命的某种业务逻辑
`28:05` 比如说我们的web application需要创建一个订单
`28:09` 一般来讲它是把董秘manager啊这一层的这种这种实现dependence injection啊
`28:18` 依赖注入到这个relication这一层
`28:20` 然后调用调用manager的相应方法来创建订单
`28:24` 删除订单啊等等等等
`28:27` 那么也是与这个application share啊这个包类似的
`28:34` 抖命这一层呢啊也需要有这么一个share的library
`28:38` 我们首先呢把这个模块啊
`28:41` 我们先给它快速的画出一个模块的方框
`28:47` 这个模块呢它的通常的命名呢是董命啊
`28:53` 这个斗命呢是基于我们具体的抖m的名字啊
`28:59` 不同的抖m呢也替换它的抖m的名字
`29:02` 这个部分这一个模块一般来讲呢会封装比较多的内容
`29:08` 这个模块呢也会是我们整个应用程序当中最最重要的数据定义
`29:15` 都定义在这个模块当中
`29:18` 首先呢它需要定义呢是我们的antity部分
`29:27` 所有的这种application和抖音之间的数据entity都define在这个部分
`29:34` 其次呢是database d t o的这种design啊
`29:40` 基于我们database的这种schema
`29:43` 我们需要定义对应的d to啊
`29:46` 也放在这个包中
`29:50` 这个地方呢比较需要注意的是
`29:53` 另外有一个非常重要的部分
`29:55` 就是我们啊d a o的interface也需要定义在这个包中
`30:02` dao呢就是我们常说的data access object
`30:07` 是用来访问数据库的一个接口
`30:11` 在这个包中我们一定要定义好它的interface
`30:16` 然后呢由infrastructure level对其进行实现
`30:21` 那么抖命层呢又会依赖于这个抖命
`30:25` share reacity呢就会直接利用这个dio的interface来进行底层数据库的访问
`30:33` 那么通过这种方式呢
`30:35` repository并不知道底层具体dao的实现方式
`30:40` 从而达到了赌命和infrastructure database
`30:45` 也就是数据访问相关的操作的一种解耦
`30:49` 接下来呢就是我们的infrastructure成的啊包
`30:53` 那么infrastructure呢
`30:54` 在这里我们假设我们采用了某某一种or m的这种啊这种内库来构
`31:02` 来帮助我们管理和数据库的相关操作
`31:06` 那么我们这里呢就需要有一个o r m的这样的包
`31:19` 那么这个o m呢需要注意的是
`31:22` o m在这里他呢对我们刚才所提到的这个dm share
`31:29` 它是一个直接的依赖关系
`31:32` 那么它呢是需要来实现dio啊
`31:35` interface
`31:37` 同样的道理
`31:39` 当我们如果有更多的第三方的内裤
`31:43` 我们呢第三方的内裤呢也需要实现他们的模块
`31:49` 同同样的道理呢
`31:50` 所有第三方的类库也需要直接引用这个董秘事里当中
`31:57` 我们对其interface的实现
`32:00` 这样的话呢董命层并不知道具体的实现方案
`32:06` 这样这样也就是我们之前提到的防腐层
`32:10` 通过这种方式呢我们可以做到啊
`32:13` 只有我们的infrastructure
`32:15` 也就是我们m还有我们的这些类库对赌命层的一种依赖
`32:20` 而赌命层呢对这些模块底层并不知情
`32:24` 没有直接的依赖关系
`32:32` 好通过目前呢这也就是一个比较完整清晰的项目结构布局
`32:38` 我们再来梳理一下他们的依赖关系
`32:41` 我们从下往上看
`32:43` 这个infrastructure呢是依赖于董明share的
`32:47` 同理董命层呢也是依赖于dmshare
`32:53` 而且我们比较顺理成章的可以看到
`32:56` application层呢是会依赖于赌命层
`32:59` 然后这其中的web层呢会依赖于application层
`33:04` 此外application层呢也会依赖于application share这个包
`33:10` 同样的抖m呢也会依赖于application share这个包
`33:16` 同样的道理
`33:17` web application也会依赖于董命share这个包
`33:22` 由于他们俩是作为最底层核心数据的定义关系
`33:28` 所以这个几乎是所有项目都会去引用的一种基础包
`33:33` 我刚才又把这一个依赖关系图重新整理一下
`33:38` 需要指出来的是
`33:40` 我们刚才呢忽略了一条依赖关系
`33:43` 也就是controller实际上也是依赖于up这个shared包的
`33:48` 因为它需要直接引用到其中的rust的d t o
`33:51` 此外呢entity这个部分应该是由赌命这个package啊所包含着的
`33:59` 因为entity包含了其中的一部分的业务逻辑
`34:03` 所以呢还有业务逻辑的部分应该被抖命这个包啊所包含
`34:10` 而dmin share这个部分呢基本只用于定义一些地to啊
`34:15` 以及相对应的一些所需要的interface
`34:19` 那么这样的话呢我们就可以看到这样一个比较啊清晰化
`34:26` 并且呢结构非常合理的软件项目的
`34:29` 那么在这里大家一定啊会比较说比较难理解的一个部分
`34:35` 就是这个抖音部分
`34:37` 抖音这个部分也是抖m driven design当中核心的核心
`34:42` 那么里头最重要的三个部分就是repository entity啊
`34:48` 和这一个董命service
`34:50` 一般呢我们以什么什么manager啊来命名的
`34:53` 这个呢也被统一称为董命service
`34:57` 但是呢有的时候service这个名字和application service啊有所重叠
`35:02` 所以呢在这里我推荐使用manager来做以区分
`35:07` 那么这三者又有什么样的区别呢
`35:11` 总体来讲
`35:12` don't mean
`35:14` 这个里头核心的业务逻辑呢是被antity和manager所管理着
`35:21` repast three呢只是单纯的作为一个数据操作的一个接口
`35:28` 那么这个就是说reacity的逻辑
`35:33` 我们要保证它逻辑越少越好
`35:37` repository应该仅仅做啊与数据操作读取
`35:43` 比如说返回id为某一个数值的啊
`35:49` resource或者是更新i d为数值
`35:53` so为某一个数值的资源的一些属性
`35:57` 仅仅做这些操作
`36:00` 那么真正的业务逻辑呢是存在于antity上
`36:05` 但是这里需要注意的是
`36:06` entity与manager的依赖关系是entity永远不应该直接使用manager或者说repose
`36:14` 对啊
`36:15` 这两个数据结构只有manager能够操纵这个entity
`36:21` 一般来讲是web这个application通过调用啊manager这样的接口拿到啊
`36:31` 以实现对其中某一项entity的创建活动
`36:37` 所以呢manager是用于调度不同安体之间
`36:44` 甚至是某一个entity与外部操作一些交互所需要的业务逻辑
`36:51` 那么entity呢只对他自己本身所需要的业务逻辑进行封装啊
`36:58` 比如说一些名字的检查
`37:01` 还有所有一些最基本的业务逻辑的数值计算
`37:06` 这些只与entity自己本身相关的那个部分是封装在这一层的
`37:17` 在基于这个架构下呢
`37:19` 可以给大家日常工作中如何启动你们的和一个新的项目也好
`37:25` 已有项目先添加一个新的功能也好
`37:29` 这都是很好的一个指导去设计你的模块的一种方向啊
`37:34` 啊但是我也想强调这个呢只是一个指导架构
`37:39` 具体的架构呢也需要依赖于你们项目的实际情况进行实际设计
`37:45` 有的时候在项目的初期阶段
`37:48` 并不需要把某些类分的太过清晰
`37:52` 这个呢但是呢我的一个建议就是永远从这个角度开始划分这些项目
`38:01` 随着项目的逐渐复杂和增大
`38:04` 这些的模块啊都一定会逐一的出现
`38:08` 那么接下来呢我们来讲一下几个斗命追问中啊
`38:13` 老生常谈的几个概念啊
`38:16` 首先呢第一呢就是entity
`38:20` entity呢很简单来讲
`38:22` 就是带有带有这个资源i d
`38:26` 以及有自己独立生命周期和状态表达的一种啊一种数据类型吧
`38:34` 可以说比如说我们的订单也好啊
`38:37` 我们的嗯某一次银行转账也好
`38:41` 这里头每一次转账订单啊
`38:44` 这些都可以是一种antity
`38:47` 那么还有一种呢叫做value object
`38:53` 那么value object一般来讲他自己没有固定的i d
`38:58` 也没有生命周期
`39:00` 它呢仅仅呢是有自己的一些属性
`39:03` 最最常见的一种value object就是我们的地址信息
`39:07` 地址信息在大部分场合下是没有id的
`39:11` 我们仅仅通过国家省份
`39:14` 城市啊
`39:15` 街道等等就能够确定一个具体的地址
`39:19` 一般呢value object它呢是包含于和entity啊
`39:25` 自己独立使用的
`39:27` 那么第下一个重要的概念呢就是aggregates
`39:35` aggregates是一种特殊的entity
`39:39` 那个这个和entity具体的区别怎么来解释呢
`39:43` aggregates更像是一种更像是一种啊超级拥有超级权力的anity
`39:52` 他呢更像是一个公司中的一种老板
`39:56` 他管理了啊
`39:57` 子底下的多种一个或者多个的entity
`40:02` 那么当一个外部的系统想要访问这个aggregates
`40:07` 下面的某一个entity的时候呢
`40:09` 是一定需要通过这个aggregates开放出来的接口来进行访问
`40:16` 所以呢我们的这个抖m driven design啊
`40:22` 所推荐的这种关于这个数据模型呢
`40:27` 它呢是基于aggregates
`40:30` 也就是说我们一个系统复杂的系统
`40:34` 第一步呢是需要进行拆解
`40:37` 将其拆解为几个复杂的独立的这种aggregates
`40:46` 然后基于每一个aggregates
`40:49` 其中呢我们再去定义好他们的这些entity
`40:58` 那么他们呢都是访问entity
`41:02` 是通过aggregates啊
`41:04` 彼此互相的这种访问啊来进行其中entity数据操作变化的一种管理
`41:14` 另外呢需要特殊注意的是
`41:17` aggregates一般呢是作为一个啊这个数据一致性的预啊来进行管理
`41:26` 也就是说这个aggregates对其中里头的操作
`41:32` 每一次进行操作的时候
`41:34` aggregates这一层啊是需要保证啊里头的数据一致性的
`41:41` 一个好的design一定是aggregates能够啊
`41:46` 只要能够保证aggregates内部的数据一致性啊
`41:50` 即可不需要有多个同同步协调
`41:55` 多个aggregates之间数据一致性的情况
`41:58` 当然了
`41:59` 这个是绝对的理想情况
`42:02` 那么在这里呢既然我们讲到了aggregates
`42:06` 那么同样呢这里很自然的啊
`42:10` 也就引入了我们的一个常规的架构啊
`42:14` 也就是我多次提到的微服务架构
`42:18` 微服务架构呢作为aggregates
`42:21` 可以是作为一个微服务架构划分的很好的理论依据
`42:26` 那么在一个理想的状态下
`42:29` 每一个aggregates都应该是一个微服务
`42:33` 那么微服务内部保证了微服务内部数据的一致性
`42:38` 微服务和微服务之间的通信呢啊也就代表了这个aggregates啊
`42:44` 之间去修改他们内部entity状态
`42:49` 是一定要是在跨服务的这种服务与服务之间的请求而来完成的
`42:57` 那么这个aggregates这一部分呢
`43:00` 其实我觉得对于大多数的实际项目
`43:03` 要求他们这么严格的划分是有一些苛刻的
`43:08` 在我个人的经验中
`43:09` 一般呢项目只初始阶段啊
`43:13` 会根据我们大致的需求
`43:15` 就能够很快地将项目化为一个2~3个之间的微服务
`43:22` 那么大大部分情况下
`43:24` 我们只需要保证这少量的微服务内部作为一个聚合根啊
`43:30` 管理了相当数量的这个
`43:35` 只有当我们其中某一个这个聚合跟变得十分庞大的时候
`43:40` 在进行中逐渐的拆分成为紫微服务
`43:44` 那aggregates这种思想呢
`43:48` 我觉得并不一定在项目项目中啊
`43:51` 一定要严格地遵循
`43:53` 只要在微服务中啊
`43:55` 能够保证彼此数据的一致性即可好了
`44:00` 这就是本期视频的主要内容
`44:02` 在这期视频中呢
`44:04` 我们主要给大家讲解了啊
`44:07` dom driven design的这种基本思路啊
`44:10` 以及它是如何对现有的常见的三层架构进行一个改造改造
`44:16` 成为这样的一种啊高内聚低耦合的这样的一种新型架构
`44:22` 并且呢基于这个新型架构提出了一种模块化
`44:27` 对其模块软件模块化的一种合理的切分
`44:31` 那么这种软件模块化的切分呢是本期视频的最最重要啊
`44:37` 想要让看到这期视频的人了解到的一个部分
`44:41` 至于这些学术词汇啊
`44:44` 嗯entity value object
`44:47` aggregate
`44:47` 大家呢听过理解到啊
`44:50` 也就好
`44:50` 在现实当中呢并不一定严格遵循这个
`44:55` 但是呢对于软件的设计
`44:58` aggregates是很好的一个切分啊
`45:02` 微服务的一个点
`45:03` 在微服务中呢大家也要记住
`45:06` 保证好数据一致性啊
`45:08` 这个一定是所有项目最最重要的一个部分
`45:12` 好好了
`45:13` 这期视频有点长
`45:14` 能够看到这里的小伙伴们一定是真爱了
`45:17` 如如果喜欢我的视频的话
`45:19` 不要忘记对我的视频进行三连哦
`45:23` 谢谢大家
`45:23` 我们下期视频再见